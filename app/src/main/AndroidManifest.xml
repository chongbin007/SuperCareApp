<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chongbin.hci_project">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

        <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
            <activity android:name=".MainActivity">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>
            <activity android:label="Hospital Location"
                android:name=".HospitalActivity"
                />
            <activity android:label="Sleep Helper"
                android:name=".SleepActivity"
                />
            <activity android:label="Forum"
                android:name=".ForumActivity"
                />
            <activity android:label="Search Results"
                android:name=".SearchActivity"
                />
            <activity android:label="Dialog with Doctor"
                android:name=".DialogActivity"
                />
            <activity android:label="Comments in Forum"
                android:name=".CommentActivity"
                />
        </application>

    </manifest>